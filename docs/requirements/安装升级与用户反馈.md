# 需求文档：安装升级与用户反馈

- 批次：2026-09-16-安装升级与用户反馈
- 状态：用户已授权实施（本任务对话）；对外地址等待提供，未配置时不可伪装已上线。
- 目标版本：1.1.0（本地基线 1.0.0 已提交）

## 一、总体目标

普通 Windows x64 用户无需安装开发工具即可安装看板、完成首次配置、收到更新提示并主动升级，通过微信群反馈问题。

## 二、用户故事

1. 下载带运行环境的中文安装 EXE，以当前用户权限安装；完成后打开原有看板配置向导。**〔2026-09-17 变更：看板内的「设置向导」已整块移除，配置入口改走本机连接窗口（`aiquotaboard://connect`）；安装本身的描述不变——见变更记录〕**
2. 保留便携 ZIP 与源码入口，普通用户只看到一个推荐的安装入口。
3. 启动及后台每天至多自动检查一次公开版本信息；支持手动检查、关闭自动检查、稍后提醒。**〔2026-09-17 变更：「稍后提醒」已撤除，更新提示只剩「跳过该版本」——按版本号记忆：跳过的那一版不再提示，发布更新的版本时照常提示；手动检查与关闭自动检查不变——见变更记录〕**
4. 更新展示版本和摘要，点击后下载并校验安装包，交由中文安装向导完成；不静默安装。**〔2026-09-17 变更：下载 → 校验 → 交安装向导 这条链路已整条撤除（不做自动下载、不做自动安装，也不建 `data/updates` 目录）；现状是只展示版本与更新说明，动作只剩「跳过该版本」。要升级请自己下安装包——以下原文仅作当时记录，见变更记录〕**
5. 升级备份旧程序、配置与历史，正常升级保留数据；安装失败回退旧程序。网络失败不影响采集。
6. 完整卸载任务、托盘、自启、快捷方式及协议；默认保留数据，删除数据须用户明确选择。
7. 软件、README、安装完成页均能找到反馈入口；固定社区页面可更换群二维码；复制反馈模板仅含版本、系统和待填问题。
8. GitHub/Gitee 同步同一组产物，更新源优先 Gitee、GitHub 兜底；尚未建立的仓库不可编造。

## 三、非功能要求

- 不上传密钥、用量、目录或匿名使用数据；R3 遥测不在本批。
- 发布前沿用审计闸门；新增独立副本测试，不能替换真实配置或停止当前开发目录的采集。
- 新模块拆为独立文件，不继续扩张配置向导内联脚本。
- 需求、设计、批次交付/差异/验证及 CHANGELOG 同步；旧批次记录不改写。
- 实机安装、平台取数、真实群二维码等未验证项目须如实列明。

## 变更记录

| 日期 | 变更点 |
|---|---|
| 2026-09-16 | 初版：本批（安装升级与用户反馈）需求三层落档（用户故事 1–8 与三条非功能要求）。 |
| 2026-09-17 | **更新链路收敛：只留「跳过该版本」（用户决定）**——撤除「稍后提醒」（24 小时免打扰）与「立即更新」（下载 → 校验 → 交安装向导）两条链路；检测到新版本时弹窗与横幅都只剩一个动作「跳过该版本」。协议动作名同批统一为 `skip`：页面 `assets/support.js`、协议白名单 `程序/运行协议.vbs`、`程序/tools/update.mjs` 三处同名（改名要三处一起改，对不上的表现是「点了没反应」）；已无执行端的安装动作从白名单删除。 |
| 2026-09-17 | **配置入口同步（同批事实）**：看板内的「设置向导」已整块移除，配置走本机连接窗口（`aiquotaboard://connect`）——用户故事 1 的就地标注按这一现状写。 |
