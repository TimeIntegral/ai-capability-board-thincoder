# 开发与发布约定

- 本目录供开发。保留用户的配置、密钥和历史，不在这里运行安装、卸载或更新安装程序。
- 完成可用改动后执行相关测试、发布预审，再保存本地代码提交；不要自动推送或公开发布。
- 测试配置与采集必须在独立副本中进行，不使用真实密钥调用平台做测试。
- 准备发布使用 `node release/prepare.mjs`，先提交再从固定提交构建独立候选版。
- 日常版安装在安装程序默认目录；配置和数据留在该安装目录，开发修改不会影响它。
- 连接平台的主要入口是本机窗口；看板页面不接收密钥。保留 ThinCoder 和环境变量自动复用。
- 发布后不覆盖旧版本附件或标签；修复后增加版本号重新构建。
