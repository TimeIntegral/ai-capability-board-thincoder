# 变更日志

版本号以仓库根目录的 `VERSION` 文件为准，遵循[语义化版本](https://semver.org/lang/zh-CN/)：
修 bug 加末位（1.0.1）、加功能加中位（1.1.0）、不兼容的大改加首位（2.0.0）。

---

## 1.3.0 — 目录整理与配置入口收敛

- **配置只有一条路**：设置向导不接触密钥值，只告诉你「哪几家已经能用」；没有密钥时引导装 ThinCoder，在里面配一次即可。
- **移除控制台问答向导**（`首次配置.bat`）：两个入口并存反而让新用户不知道该用哪个。
- **安装目录整理**：要双击的脚本全部收进「快捷操作」，程序代码收进「程序」；顶层从 24 个散文件降到 12 个，并新增「先看我.txt」说明哪些要管、哪些别动。
- **不再教用户改文件**：删掉「编辑密钥文件 / 编辑配置文件」这类入口；浏览器不支持时直接引导换 Edge 或 Chrome，而不是让用户去改配置。
- **安装界面不再推销社区**：完成页只保留「打开看板」；用户群与反馈入口在看板右上角「帮助与反馈」里。
- **文案继续改大白话**：向导每一步改成「一句做什么 + 一句会怎样」，检查项写明具体查什么（例：「正在检查：Codex 登录了吗、DeepSeek / GLM 的密钥找得到吗」）。
- 升级说明：安装程序版直接升级即可；**ZIP 覆盖升级的用户需双击一次「快捷操作 → 安装定时任务.bat」**，否则采集会静默停止。

## 1.2.0 — 简化连接与发布隔离

- 新增本机连接窗口：选择平台、自动复用已有账号、按需补充密钥、保存并验证连接。
- 看板页面不接触密钥，不再要求新用户选择文件夹或手工编辑配置；旧版向导保留为高级入口。
- 明确 Codex 订阅额度与 DeepSeek / GLM API 余额的区别，一家连接失败不阻塞其他平台。
- 开发目录禁止后台安装与自动升级；正式构建要求干净提交，候选版从固定提交的独立副本生成。
- 候选包附代码快照记录；本地提交、构建候选版、公开发布分别管理。

## 1.1.0 — 安装、升级与用户反馈

- 新增 Windows x64 中文安装包：内置运行环境，当前用户安装，统一启动入口；保留便携 ZIP。
- 新增更新与反馈面板：每日检查、手动检查、稍后提醒、托盘提示开关；GitHub 发布信息驱动，预留 Gitee。
- 更新包校验大小、文件类型与 SHA256，来源限定本项目；点击后才启动升级向导，失败继续使用原版本。
- 升级前备份程序、配置和历史，正常升级不覆盖私人数据；安装失败尝试恢复旧程序，保留备份。
- 完整卸载所属计划任务、托盘、自启、快捷方式及协议；默认保留数据，额外删除须用户选择。
- 新增固定社区页面、问题入口及不含密钥/用量/项目路径的反馈模板。微信群尚未开放。
- 测试在独立副本执行，避免旧测试与真实采集争抢配置。具体执行证据见本批次档。
- **首次配置的每一步都改成大白话**：把「选项目文件夹 / 检测本机能力 / 试采一次」这类内部叫法换成「看板在哪儿 / 看看能用的 / 先拉一次数据」；不再把 `.vbs` `.bat` 这类文件名扔给用户，改成「找到名字叫『启用自动采集』的那个文件双击」（并先说明「不会弹黑窗口、也不会装别的东西」）。

## 1.0.0 — 首个公开发布

把 Codex / DeepSeek / GLM 三家的额度、用量与去向集中到一个本地看板，并且回答**只有本地数据才能证明**的那些问题。首个公开版本，按主题列出。

### 额度与用量

- **Codex**：走官方后端接口取实时额度（5 小时窗口 / 7 天窗口），失败时回退到本地会话快照并标记「降级」；口径统一为**剩余额度**。
- **DeepSeek / GLM**：官方与半官方余额接口，含消耗速率与「按当前速率还能用几天」的推算；GLM 额外解析充值资源包的到期状态。
- **分平台采集间隔**：计划任务按最小间隔触发，各平台按自己的间隔取数（未到期沿用上次结果，看板标注「沿用上次」与数据年龄）。

### 只有本地数据能给出的视角

- **额度去向**：解析本地会话记录，按**项目目录**聚合 token 用量——额度被哪个项目吃掉了。这是官方页面给不了的信息。
- **模型使用画像**：接口只告诉你"哪些模型可用"，这里回答"实际在用哪个、用在哪、什么时候换的"。含活跃区间与模型切换轨迹。
- **使用方式画像**：权限模式 / 沙箱策略 / 推理强度的分布，以及平均每轮消耗。
- **ThinCoder 项目索引**：所有用过 ThinCoder 的项目及其会话数与轮次。

### 看板

- 自研 canvas 图表引擎：悬停十字准线与数值提示、X 轴时间刻度、阈值虚线、告警时刻标记。
- **点选区间统计**：在趋势图上点两下选区，给出该区间的峰值 / 均值 / 超阈值时长（Codex）或余额变化 / 净消耗 / 日均消耗 / 预计可用天数（余额）。
- **每日热力图**：GitHub 风格日历网格，带星期轴与月份轴，三种指标可切。
- **余额变化拐点**：每个变化起点画竖线 + 圆点，下方列出最近几次变化的时刻与金额。
- 深浅色主题（跟随系统 / 手动切换，选择持久保存）、无闪烁自动刷新、暂停、导出 CSV。

### 提醒

- Windows 原生通知，按场景分级：阈值预警、额度见顶预测、额度释放、限流、采集异常与降级、余额告急与耗尽、订阅闲置、当日异常消耗。
- 通知上带按钮（打开看板 / 去充值 / 不再提醒该平台余额），经自定义 URL 协议分发。
- **免打扰时段**（P0 级仍即时送达）与**静音**（可配置是否放行 P0）。
- **反刷屏设计**：滞回按分钟配置（改采集频率时灵敏度不变）、同类提醒共用固定 key、连续计数器带样本幂等守卫。

### 安装与分发

- 一键安装：注册计划任务（开机自动恢复、错过的点补跑、隐藏运行）、桌面快捷方式、通知应用名、URL 协议、托盘常驻图标。
- **内置便携 Node**：发布包可自带运行时，没装 Node 也能解压即用；安装与采集脚本优先使用它。
- **平台开关**：只用其中一两家也可以，关掉的平台不采集、不显示、不报错。
- **密钥来源三级**：`secrets.json`（推荐）→ 环境变量 → 兼容从 ThinCoder 配置读取。采集运行时**只读**密钥；唯一的写入路径是你在看板上点保存时（见「看板内配置」）。密钥永不进日志、状态文件或看板。

### 工程与安全

- 零外部依赖（只用 Node 内置模块），Windows 原生脚本，无服务端、不联网上传任何数据。
- 数据**默认永久保留**，用分级降采样控制体积：7 天内原始粒度 → 5 分钟 → 1 小时 → 1 天；不删除任何数据点。
- 采集健康面板：每平台近 24 小时成功率、采集次数、平均耗时与最近错误——数据存疑时可自证清白。
- 错误信息统一脱敏：命令行的鉴权参数、密钥值不会进入日志、状态文件或看板。

### 看板内配置（首次配置不用碰命令行）

- **打开看板就能配完**：选平台 → 填密钥 → 写配置 → 试采验证 → 开启自动采集，全程在页面上完成，不弹控制台窗口。
- **页面直写配置**：经浏览器目录授权（File System Access）直接写 `secrets.json` 与 `config.json`；**未知字段原样保留**，只覆盖你填的那几项。
- **坏文件不吞掉**：配置损坏时显式提示「备份并重建」，把原文件留成 `.broken.bak` 再写干净版本；同时在看板上告诉你「当前按默认设置运行、自定义已失效」。
- **探测本机已有能力**：新增探测脚本盘点各平台当下能不能采到（Codex 登录状态、两家密钥来源），看板据此给建议；自动采集是否已开启以计划任务实查为准，不做猜测。
- **一键启用自动采集**：装过的一键开启；全新副本引导双击一个**无窗口**的静默脚本（完成提示为中文，含失败与「找不到安装脚本」两种情形）。
- **兜底路径保留**：浏览器不支持目录授权、或你不想在页面上填密钥时，仍可走记事本 / 控制台向导。
- **密钥纪律**：密钥只存在于密码输入框、页面临时对象与 `secrets.json` 三处；不进 URL、进程命令行、浏览器存储、DOM 序列化、日志或看板数据。

### 许可

以 **AGPL-3.0** 发布。自己用、改、给朋友都没问题；若修改后对外提供服务，需要开源你的改动。

---

## 开发历程（0.1.0 → 0.9.0）

从"三平台额度能不能集中到一个看板"开始，一周内迭代到 0.9.0。下面是这段过程中值得留下的技术决定与踩坑，按主题重述——**完整开发日志（含真实数据）不在公开仓库里**。

### 采集层

- **Node 的 `fetch` 不走系统代理**，所以 Codex 的实时查询改用系统 `curl`，并按「代理 → 直连 → 再试一次」的顺序尝试；失败则回退到本地会话快照（标为降级，而不是显示错误）。
- 超时统一收紧到 12 秒，保证单次运行远小于触发间隔；计划任务用 `IgnoreNew` 防止重叠执行。
- GLM 的额度接口是对网页前端逆向得到的非公开接口，代码里对"它随时可能失效"做了降级处理：失败只降级、不影响其他平台。
- **单平台没配密钥不算故障**：只标记「未配置」，不拖垮整次采集，也不触发告警。

### 规则引擎（踩坑最深的一段）

- **绝不把易变的时间戳当作实体身份。** 早期把某个重置时间戳嵌进提醒 key，而那个值其实是滑动的，导致每次求值都生成全新 key、冷却机制完全失效——连续数日每几分钟重复弹同一条提醒，提醒历史里几乎全是噪音。修正后的纪律是：规则只依赖**状态自身的走势**，key 用固定命名空间。
- 三条基础语义：**峰值回落**（用量从高位回落到低位且降幅足够）、**armed + 滞回**（阈值）、**边沿 + 滞回**（限流、降级恢复）。
- 连续计数器类规则必须有**样本幂等守卫**：记住上一次求值的样本时间，同一样本重复求值直接返回，否则并发采集会重复累加、提前触发。
- 状态类提醒的「降级」与「恢复」**共用同一个 key**，避免抖动时刷屏。
- 滞回按**分钟**配置、按各平台实际间隔换算成样本数——采集频率从 5 分钟改成 1 分钟时告警灵敏度不变（早期用固定样本数，改频率后过敏了数倍）。
- 测试用**真实历史回放**：把本地历史逐样本喂给规则引擎，断言"不产生刷屏类提醒""同一提醒的最小间隔 ≥ 冷却时间""提醒密度在合理范围"。注意最后一条要用**密度**而不是绝对条数——绝对上限会随历史变长而必然触顶，那时它反映的是数据增长而不是缺陷。

### 看板

- 图表引擎自研（零依赖）：悬停十字准线、时间刻度去重、阈值虚线、告警时刻竖线、平值序列给固定跨度（防止小数值变化被视觉放大）。
- 数据刷新用**动态替换 script 标签**，避免整页重载闪烁；主题选择存 localStorage，且手动选择优先于配置，防止每次刷新被重置。
- Codex 卡片区分**窗口活跃**与**窗口已结束**两种状态：不使用时窗口会冻结，此时显示一个已经过去的"刷新时刻"会误导。
- 热力图最初只有色块、没有坐标，无法定位到"哪一周、哪一天"；补上左侧星期轴与顶部月份轴，并对齐做了几何断言。

### 数据层

- **增量扫描**：会话文件是只追加的，按文件缓存「大小 + 已消费字节偏移 + 按日聚合」，下次只读新增的尾巴，文件没变大就完全不读。全量重建约 10 秒 → 之后每次维护约 0.01 秒 / 0 字节。
- 增量缓存带**版本号**：解析逻辑新增字段时必须升版本，否则复用旧缓存会静默缺字段（踩过一次：新增"首次使用时间"后，所有模型的那一列都是空的）。
- 大文件读取改为尾部读取 + 按需扩窗，避免为了取最近几行而把整个文件读进内存。
- 历史数据按时间桶分级降采样：每档保留桶内最新样本，**不删除任何数据点**。

### Windows 集成的坑（都踩过）

- **`.vbs` 必须保持纯 ASCII**：Windows 脚本宿主按系统 ANSI 代码页读取，UTF-8 中文注释会吞掉换行、破坏解析（表现为莫名其妙的语法错误）。
- **ShellExecute 会规范化 URL**：`scheme://action?x` 会被改写成 `scheme://action/?x`（在 `?` 前插入斜杠），协议处理器不剥离这个尾随斜杠，所有按钮都会静默失效。
- **`fs.cpSync` 在中文路径下会让 Node 原生崩溃**（进程直接异常退出），批量拷贝一律改用逐文件 `copyFileSync`。
- JS 模板字符串会把 Windows 路径里的反斜杠当转义吃掉，写注册表要用参数数组而不是拼接命令。
- `findstr` / `reg query` 的中文比对受控制台代码页影响，会得到"假故障"，改用 PowerShell 或码点比对。
- 通知要显示成应用自己的名字，需要注册 AppUserModelID，否则一律显示为 Windows PowerShell。

### 分发准备

- 发布前审计：机械扫描所有将被提交的文件，命中密钥模式、个人信息、绝对路径或禁入文件就**拒绝发布**。
- 私有区隔离：本地笔记、完整开发日志、运行时数据、密钥全部物理隔离在 git 之外，靠 `.gitignore` 与审计脚本双重保证。
- 便携运行时：二进制从国内镜像下载、校验和从官方源获取（两者分离，镜像被换包也过不了校验），校验通过才落盘。

---

*公开仓库只包含发布版；本地开发日志、真实数据与个人配置不在其中。*
